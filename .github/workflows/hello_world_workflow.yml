name: hello_world_workflow

on:
  push:
    branches:
      -develop

  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest

    steps:
      -  name: Say Hello
         run: | 
           echo "Hello World"
      
